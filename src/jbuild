(library
  ((name example01_lib)
   ;; Without the following line, the libraries module will be available
   ;; under Example01_lib.Module_name only
   (wrapped false)
   (synopsis "Description for this library...")))
